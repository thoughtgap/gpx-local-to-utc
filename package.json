{
  "name": "timezone-corrector",
  "version": "1.0.0",
  "description": "Converts local times of a GPX file into proper UTC",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "geo-tz": "^8.0.1",
    "moment": "^2.30.1",
    "moment-timezone": "^0.5.45",
    "xml2js": "^0.6.2"
  }
}
